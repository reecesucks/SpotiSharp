namespace SpotiSharp.Models;

public enum RadioAlbumMode
{
    Off = 0,

    // the album's songs are sprinkled individually through the radio
    Scattered = 1,

    // the album's songs appear back to back as one block
    Consecutive = 2
}
